// SkillSphere Controllers Placeholder
// Save route controllers here (e.g. auth.controller.js, gig.controller.js)
export const placeholderController = (req, res) => {
  res.status(200).json({ message: 'Controller placeholder active' });
};
